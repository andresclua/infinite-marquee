{
  "name": "@andresclua/infinite-marquee-gsap",
  "version": "0.0.4",
  "main": "index.js",
  "author": "Andres Clua",
  "license": "MIT",
  "scripts": {
    "dev": "parcel -p 3000 ./src/*.html",
    "build": "parcel build ./src/*.html",
    "predeploy": "rm -rf dist && parcel build --no-scope-hoist --no-source-maps src/*.html --public-url https://andresclua.github.io/infinite-marquee/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.12"
  },
  "dependencies": {
    "gsap": "^3.11.4"
  }
}
